use bevy::app::{App, Plugin};

// TODO: support sprite atlas, and document this.
/// Handles the preview for sprites.
pub struct SpriteAssetPreviewPlugin;

impl Plugin for SpriteAssetPreviewPlugin {
    fn build(&self, app: &mut App) {}
}

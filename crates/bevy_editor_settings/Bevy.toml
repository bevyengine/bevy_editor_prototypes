
# basic struct
[basic_settings]
name = "bevy_editor_settings"


# list with replace
[list_testing]
list = ["three", "four"]

# list with append
[list_testing_append]
list = [3, 4]

# top level enum
[enum_testing]
variant = "Two"

# enum in a struct
[enum_settings]
setting = "Three"
# can also be written as
# [enum_settings.setting]
# variant = "Three"

[enum_settings_list]
settings = ["Three"]